# Figma 三遗留端点（mark_diagram_inserted / claim / apply_hub_file_collections）结论（r210，2026-09-11）

> 面①：`PUT /api/file/mark_diagram_inserted/{fileKey}` —— 单数族遗留（0 历史覆盖）
> 面②：`PUT /api/file/claim/{fileKey}` —— 仅 v56 语义 3 发（另有 r130 的 GET 误打 1 发=无效）
> 面③：`POST /api/files/{fk}/apply_hub_file_collections`（body `source_hub_file_id`）—— 仅 r38 功能 1 发 + r69 null
> 方法：JS 逆向调用链/schema（`_tmp_r210_ctx/b/c`）→ 历史覆盖甄别（`_tmp_r210d/e`）→ 探活两波（probe1/2）→ 核心判别（probe3/4）→ WS 幂等观察
> 结论：**三面全负（无 SQLi 行为信号：前置门 ×2 + parseInt 清洗 ×1）；无提交物。** 新增 1 条方法论（路由与前置可达性判别：404 形态学/活性探针/前置门判死法）、1 条构造（数字位清洗型/拼接型布尔对仲裁）、1 条镜像判据（字符串 key 位精确匹配）。

---

## 一、侦察（JS 逆向，r210 系列）

- 三端点调用点（figma_app-main.js @180641 模块）：
  - `markInserted(e){return l.validate(async({xr:t})=>await t.put(t.url`/api/file/mark_diagram_inserted/${e.fileKey}`))}`，响应 schema `{success:boolean}`（MarkInsertedSchemaValidator）。
  - `claimTryFile(e){...e.folderId?await t.put(url,{folder_id:e.folderId}):await t.put(url)}`（双分支）；body schema `{save_location:string, folder_name?, org_id?}`（与实发不符——实发只有 folder_id 或空 body）。
  - `applyHubFileCollections({sourceHubFileId:e,destinationFileKey:t}){...await a.Ay.post(a.Ay.url`/api/files/${t}/apply_hub_file_collections`,{source_hub_file_id:e})}`。
- 历史覆盖甄别：markInserted 0；claim 仅 v56 语义（T1 自有 FILE_A / T3 不存在 / T4 anon）；apply_hub 仅 r38 功能使用（真 hub 1633508841091684550 → k7tno 成功复制）+ r69 null 探针。
- 关联族（本域生态，r211 候选）：`/api/hub_files/v2/{id}/copy`、`/api/hub_files/template/{id}/copy`、`/api/hub_files/{id}/{version}/prepare_insert`、`/api/hub_files/{id}/template_canvas`、`/api/hub_files/{id}`（DELETE）、`/api/resource_uses/hub_file/{id}`、`/api/templates/...`、`/api/files/{fk}/workshop`。

---

## 二、面① mark_diagram_inserted（PUT 无 body，12 发）

### 2.1 路由活性判别（新方法论首用）

| 发 | 请求 | 结果 | 判读 |
|---|---|---|---|
| B8 | OPTIONS `/mark_diagram_inserted/{NE}` | 404 · 826KB HTML（SPA 兜底页） | 方法未进 API 层 |
| B9 | POST 同路径 | 404 · 826KB HTML 同 | 同上 |
| B10 | PUT + 坏 JSON（`{`）打到真 FK2 | 400 `{"error":"Failed to parse body as JSON"}` | **body 解析中间件活跃 + 路由已接** |
⇒ PUT 路由存在于 API 层；业务全 404 = 前置条件（AI diagram 生成 job 在途）未满足，非路由缺失。

### 2.2 业务矩阵

- B1 不存在 key（20×z）/ B2 真 FK2 / B3 FK2 重放 / B11 k7tno（make 文件）/ B12 FILE_A（design）：**全 404 `error.not_found` 同态**（含真文件！）。
- B4 `'` / B5 `''` / B6 `'--` / B7 `';SELECT pg_sleep(3)--`：全 404 同态、零时延（319-670ms）。
- **判定**：真值也 404 ⇒ 前置门（非判别面问题）；全形态零差分 ⇒ 注入判别面不可建立；**判死（本资产不可达）**。

---

## 三、面② claim（PUT，11 发）

- C1-C5（不存在 key + plain/`'`/`''`/`'--`/sleep）：全 400 同一文案 `Try file does not exist, is deleted or no longer in workshop mode so cannot be claimed`、零时延（322-344ms）。
- C6-C10（k7tno make 文件 + 同五形态）：全 400 同文案。
- C11（FILE_A design 文件）：400 同文案。
- **判定**：workshop/try 文件资产不可得（无 try 文件）→ 统一错误文案（防枚举）→ **判死**。

---

## 四、面③ apply_hub_file_collections（POST，20 发）

### 4.1 body 位 source_hub_file_id（数字语义）

| 发 | 值 | 结果 | 判读 |
|---|---|---|---|
| D1 | JSON 数字 `1633508841091684551` | 400 `Error in route typechecking ... Expected string` | 路由 schema=string |
| D8 | 字符串 `"1633508841091684551"` | 404 | 值不存在 → 业务判空 |
| D2-D5 | HUB1 + `'`/`''`/`'--`/sleep | 全 404 同态 | 不存在值上全同构（**无判别力**） |
| D9/D11 | **真值** + `'` / `''` | **200 ×2** | **尾缀被吞（真值被解析出）** |
| D10 | 真值原样 | 200 | 成功基线 |
| P4/P5 | 真值 + `' AND '1'='2` / `' AND '1'='1` | **200 ×2（布尔对零分叉）** | **清洗型（值纯数字化，无 SQL 语义）** |
| P1/P2 | 真值+`abc` / `abc`+真值 | 200 / 404 | **parseInt 前缀语义实锤**（尾部非数字吞、前缀非数字败） |

- **判定**：输入被 parseInt 化为整数后使用 → **无注入面**。

### 4.2 路径位 destination_file_key（字符串 key）

- Q1 `FK2'` / Q2 `FK2''` / Q3 `NE'` / Q5 `FK2'--` / Q6 `FK2';SELECT pg_sleep(3)--`：**全 403 no_permission 同态**、零时延（319-636ms）。
- **镜像判据**：若该位是清洗/前缀吞没型 ⇒ `FK2'` 应被吞回真 key 得 200；实为 403 ⇒ **精确匹配（参数化/严格查找）** → 无注入面。

### 4.3 应用效果观察（测试卫生）

- FK2 经 **6 次 200**（D9/D10/D11/P4/P5/P1）后 WS `ListCollectionsView`：`fileCmsCollections initial: {}`（**空**）。
- 对照 RuEdaohBLXN48WdkzS66BY（r194b 集合宿主）WS 返回多集合（非空）→ 方法有效性验证 ⇒ **FK2 无任何集合落地**（200 = 静默 no-op 或目标无 CMS 通道；零痕迹）。
- `GET /api/collections/{CID}/export` 复核：200 存活（145B CSV），注入探针字段名原样（如 `f3' OR '1'='1--`）= 历史"字面存储"结论复现。

---

## 五、机制总表与对账

| 面/位置 | 判别力 | 注入族结果 | 机制归属 | 对账 |
|---|---|---|---|---|
| markInserted fk | **不可建立**（真值也 404） | 全 404 零差分 | 前置门（AI job）未满足 | 判死 [x] |
| claim fk | **不可建立**（统一 400 文案） | 全同构零时延 | 前置门（workshop 模式）未满足 | 判死 [x] |
| apply_hub body | 激活（真值 200 / 假值 404） | 布尔对零分叉 | **parseInt 清洗**（数字规范化） | V2 [x] |
| apply_hub path | 激活（403 门） | 全 403 同构 | 精确匹配（参数化） | V1 镜像 [x] |

---

## 六、画像与缺陷记录（记录，不提交）

1. **apply_hub 宽松解析（parseInt 前缀）**：非注入缺陷；"数字位宽容解析"是常规注入预警面，本端点下游为纯数字使用，无面。
2. **FK2 6 次 apply 200 无落地**：幂等/静默 no-op 行为画像（无副作用残留）。
3. **404 三形态共存**：SPA HTML 兜底（方法/路径未进 API）/ JSON `error.not_found`（业务层）/ 纯文本 `Not Found`（API 路由级）——路由活性指纹。
4. **hub_files 族新面**：copy / prepare_insert / template_canvas / resource_uses + templates 族 + `files/{fk}/workshop` 未见历史测试（r211 候选）。

---

## 七、资产

- 脚本：`_figma_r210_probe.py` / `_figma_r210_probe2.py` / `_figma_r210_probe3.py` / `_figma_r210_probe4.py` / `_figma_r210_ws.py` / `_figma_r210_diag.py`
- JS 逆向：`_tmp_r210_ctx.py` / `_tmp_r210b_callers.py` / `_tmp_r210c_more.py` / `_tmp_r210d_cover.py` / `_tmp_r210e_check.py` / `_tmp_r210f_js2.py` / `_tmp_r210g_hubctx.py`
- 落盘：`_r210p_*.txt`（20）/ `_r210p2_*.txt`（14）/ `_r210p3_*.txt`（3）/ `_r210p4_*.txt`（8）/ `_r210_ws_collections.txt` / `_r210g_hubctx_out.txt`
- 上下文产出：`_r210_ctx_out.txt` / `_r210b_ctx_out.txt` / `_r210c_ctx_out.txt`

## 八、回流（已执行，见 CHANGELOG 2026-09-11 r210）

03 二·补二（路由与前置可达性判别，新节）；03 一（SPA 兜底页行）；03 六·补（数字位清洗型/拼接型仲裁行）；01 V1（路径 key 位镜像判据）；01 V2（真值清洗三针对）；05 案例 11；总账附录（2026-09-11）。
