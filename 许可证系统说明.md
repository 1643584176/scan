# 🔑 许可证管理系统使用指南

## 📋 概述

许可证系统让你可以**控制谁可以使用共享记忆功能**，实现商业化或权限管理。

---

## 🎯 核心功能

### **1. 功能开关控制**
- ✅ 共享记忆（贡献经验）
- ✅ 下载智慧（获取社区数据）
- ✅ 高级分析（AI深度分析）
- ✅ 优先支持（VIP服务）

### **2. 许可证类型**

| 类型 | 共享记忆 | 下载智慧 | 高级分析 | 有效期 | 适用场景 |
|------|---------|---------|---------|--------|----------|
| **试用版** (trial) | ❌ | ✅ | ❌ | 7天 | 新用户体验 |
| **基础版** (basic) | ✅ | ✅ | ❌ | 365天 | 个人用户 |
| **专业版** (professional) | ✅ | ✅ | ✅ | 365天 | 专业安全研究员 |
| **企业版** (enterprise) | ✅ | ✅ | ✅ | 永久 | 企业团队 |

### **3. 安全特性**
- 🔐 硬件绑定（可选）
- 📅 有效期控制
- 🔢 激活次数限制
- ✍️ 数字签名验证

---

## 🚀 快速开始

### **作为管理员（生成许可证）**

```bash
python license_manager.py
# 选择 "3. 生成许可证密钥（管理员）"
# 输入管理员密码（默认: admin123）
# 选择许可证类型和有效期
```

**示例输出：**
```
✅ 许可证密钥生成成功！

许可证类型: BASIC
有效期: 365 天
最大激活次数: 1

密钥:
YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXoxMjM0NTY3ODkw...

请将此密钥发送给客户
```

### **作为用户（激活许可证）**

```bash
python license_manager.py
# 选择 "1. 激活许可证"
# 粘贴收到的密钥
```

**成功提示：**
```
✅ 许可证激活成功！类型: BASIC, 有效期至: 2027-05-09 14:30:00
```

---

## 💼 商业场景示例

### **场景1：免费试用 + 付费升级**

```bash
# 1. 给新用户发放试用许可证（7天）
python license_manager.py -> 选择3
# 类型: trial, 天数: 7

# 2. 试用期结束后，用户付费购买基础版
python license_manager.py -> 选择3
# 类型: basic, 天数: 365

# 3. 发送新密钥给用户
```

### **场景2：分级定价**

```
基础版: ¥99/年   - 共享+下载
专业版: ¥299/年  - 共享+下载+高级分析
企业版: ¥999     - 永久+所有功能+技术支持
```

### **场景3：团队授权**

```bash
# 生成可激活多次的企业许可证
python license_manager.py -> 选择3
# 类型: enterprise
# 最大激活次数: 10（允许10台设备）
```

---

## 🔧 技术细节

### **许可证文件格式**

`license.json` 存储在项目根目录：

```json
{
  "activated": true,
  "license_key": "encoded_key_here",
  "license_type": "basic",
  "activated_at": "2026-05-09 14:30:00",
  "expires_at": "2027-05-09 14:30:00",
  "features": {
    "share_memory": true,
    "download_wisdom": true,
    "advanced_analysis": false,
    "priority_support": false
  },
  "hardware_id": "a1b2c3d4e5f6...",
  "max_activations": 1,
  "activation_count": 1
}
```

### **密钥结构**

```
原始内容: basic|365|1|2026-05-09T14:30:00|abc123def456
签名: sha256(原始内容 + 密钥)[:16]
最终: base64编码(原始内容|签名)
```

### **验证流程**

```
1. 解码Base64
2. 分离内容和签名
3. 重新计算签名
4. 对比签名是否一致
5. 检查有效期
6. 检查激活次数
7. 返回功能权限
```

---

## 🛡️ 安全建议

### **1. 修改默认密码**

编辑 `license_manager.py` 第328行：

```python
if password == "admin123":  # ← 改成你的强密码
```

### **2. 修改签名密钥**

编辑 `license_manager.py` 第144行和第185行：

```python
secret = "YourSecretKey2026"  # ← 改成你的随机字符串
```

### **3. 启用硬件绑定（可选）**

当前已实现硬件ID记录，如需强制绑定，在 `activate()` 方法中添加：

```python
# 检查硬件ID是否匹配
if self.license['hardware_id'] and \
   self.license['hardware_id'] != self.get_hardware_id():
    return False, "许可证已绑定到其他设备"
```

### **4. 定期更换密钥**

建议每3-6个月更换一次签名密钥，旧许可证继续有效，新生成的使用新密钥。

---

## 📊 管理命令

### **查看所有许可证状态**

```bash
python license_manager.py -> 选择2
```

### **停用许可证**

```bash
python license_manager.py -> 选择4
```

### **批量生成许可证**

创建脚本 `generate_bulk.py`：

```python
from license_manager import LicenseManager

manager = LicenseManager()

# 生成10个基础版许可证
for i in range(10):
    key = manager.generate_license_key('basic', 365, 1)
    with open(f'licenses/basic_{i+1}.txt', 'w') as f:
        f.write(key)

print("生成完成！")
```

---

## ❓ 常见问题

### Q1: 如何防止密钥泄露？
**A:** 
- 每个用户唯一密钥
- 限制激活次数
- 硬件绑定
- 定期更换签名密钥

### Q2: 用户可以破解吗？
**A:** 
- 密钥经过数字签名，篡改会失效
- 代码可以混淆增加难度
- 结合服务器端验证更安全

### Q3: 如何续费？
**A:** 
生成新的许可证密钥发给用户，用户重新激活即可。

### Q4: 可以退款吗？
**A:** 
运行 `python license_manager.py` 选择 "4. 停用许可证"

### Q5: 如何查看谁激活了？
**A:** 
当前版本本地存储，企业版可添加服务器端日志记录。

---

## 🔄 集成到现有系统

许可证检查已自动集成到：

- ✅ `share_memory.py` - 贡献和下载时检查
- ✅ `import_community.py` - 导入时检查
- ✅ `quick_start.py` - 显示许可证状态

**无需额外配置，开箱即用！**

---

## 💡 最佳实践

### **对于个人开发者：**
1. 提供7天试用版吸引用户
2. 基础版定价亲民（¥99-199/年）
3. 通过GitHub/Gitee分发密钥

### **对于小团队：**
1. 专业版包含所有功能（¥299-499/年）
2. 提供团队折扣（5用户起）
3. 使用微信群/QQ群技术支持

### **对于企业：**
1. 企业版永久授权（¥999-2999）
2. 提供定制开发服务
3. SLA服务保障
4. 私有化部署选项

---

## 🎯 总结

通过许可证系统，你可以：

- ✅ **控制访问** - 决定谁能使用什么功能
- ✅ **商业模式** - 免费试用 + 付费升级
- ✅ **灵活定价** - 不同等级不同价格
- ✅ **安全保障** - 数字签名 + 硬件绑定
- ✅ **易于管理** - 命令行工具一键操作

**现在，你完全掌控了你的AI系统！** 🔑✨
