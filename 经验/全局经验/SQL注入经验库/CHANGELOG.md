# CHANGELOG(经验回流记录)

> 规则:每次项目测试收口后,把新经验追加到 01/03/05,并在此登记。
> 格式:`日期 | 项目 | 新增内容 | 落位文件`

---

## 2026-09-10 · v1.0 建立(Figma 战役后)

**初始装载内容**:
- ~~`01-技术矩阵.md`:P/Q/R/S/T/U 六组 42 条技术~~(来源:H1 analytics DSL 全参数实战 + Figma CMS/folder_search 全矩阵实战 + 通用方法论;**当日重构拆分迁移,见下**)
- `02-方法论.md`:判别面校准三步法、快照差分纪律、对照设计、层间解码地图、零破坏流程、全技术对账(全部有实战出处)
- `03-判读库.md`:错误消息六层指纹、状态码语义矩阵、结果三分法、读回验证语义、物理证据清单、噪声清单、收口措辞标准
- `04-位置侦察.md`:位置盘点、参数名三来源、JS 逆向三招、CRUD 五件套建模、通道盘点、宽容解析探测
- `05-实战案例.md`:Figma folder_search / CMS items / recent_search / H1 DSL 四案例全证据链
- `脚本模板/_sqli_matrix_template.py`:入参矩阵可执行探针(改 CONFIG 即打)

**关键来源事件**:
- 判别面校准(踩坑→方法论):Figma 空集误打 → r96 激活三态 → 固化为 02 判别面校准(三步法)。
- 动态字段剥离(踩坑→方法论):r85 假差分 → r87/r98 证伪 score 噪声 → 固化为 02 快照与差分纪律。
- 全技术对账(用户要求→机制):"md 全技术必须全用"教训 → 固化为 README 强制流程 + 对账机制。
- 测点新增(本战役新发现):A1 数组语法(Rails 解析层)、B6 JSON 转义层间(手工文本构造)、B7 null 四形态、C1 类型混淆三结局、消息指纹、命名规范。

---

## 2026-09-10 · Figma 新面首战(folders 域)

- 新增技术:`C2 参数名映射探测(原R12,外部名↔内部名)` —— 数组语法逼出内部名(file_type→editor_type)、内部名直发对照;→ 06-送达与过障 C 族
- 新增技术:`C3 归一化剥离探测(原R13,trim/\x00 剥离/大小写)` —— 中间插入法区分剥离与截断;→ 06-送达与过障 C 族
- 新增判据:`防御处置四模式`(白名单拒绝/静默回退/清洗存活/类型拒绝)、`归一化行为(预处理证据)`、参数映射层与白名单校验层消息指纹;→ 03-判读库
- 新增案例:`folders/paginated_files`(白名单={name,touched_at,created_at}、file_type 枚举静默回退、HPP 取尾、page_size 类型校验);→ 05-实战案例 案例5
- 流程例证:先用 JS 逆向测绘参数与合法值→判别面校准→矩阵→对账(方法链条完整跑通)

---

## 2026-09-10 · 结构重构(对齐"SQL 入参"主轴)

**背景**:库的主轴确认为 **SQL 入参**——"每个输入进 SQL 的哪个位置(值/标识符/排序…)、怎么构造逃逸";URL/编码/参数语法只是"送达通道",不作主线。

**变化**:
- **新建 `01-SQL入参矩阵.md`(★核心)**:13 类槽位(V1-V6 值位 / I1-I4 标识符位 / F1 函数 / M1 多语句 / X1 二级)× 合法性断言 × 逃逸构造 × payload 库;含**入参清单模板**(参数→内部名→槽位→定位证据→结果)。
- **新建 `06-送达与过障.md`**:原 R 组 + 编码形态重组为 A 结构语法 / B 编码族(B8 关键字变形、B9 边缘字符为本次补充)/ C 类型混淆(C2=原R12、C3=原R13)/ D 组合策略(含 D0 回显探针)。
- `02/03/04/05` 的编号引用全部迁移;收口对账表改为"**槽位覆盖 + 通道覆盖**"双层(见 02 六节)。
- 原 `01-技术矩阵.md` 拆分迁移完毕后删除(去向:P/Q→01 槽位 payload;R→06;S→I 槽位;T/U→02/03)。

**维护规则**:新经验按两个维度追加——①"槽位与构造"进 01;②"送达形态"进 06。HTTP 语法类经验一律进 06。

---

## 2026-09-10 · Figma 新面二(r129-r141:时间盲注判别 + versions/游标族)

- **新增构造**:`keyset 游标参数组(column/before/secondary_column/secondary_before)`——复合位(I1+V3+V2+V4)逐身份打 + 顺序判据 + 时间盲注终审 → 01-SQL入参矩阵(I4 后"附"节)
- **新增送达**:`D1 时间盲注送达(崩因判别/终极否决权)`——pg_sleep 嵌入合法表达式;快=解析层拦、慢=SQL 执行;对每个 500 定性 → 06-送达与过障 D 族
- **新增判据**:`游标校验层` 指纹(Invalid column type in cursor object / Endpoint requires secondary_column cursor) → 03-判读库指纹表
- **新增判据**:`五·补 500 归因:时间维判别(必做步骤)`——无时间维证据的 500 不得写入注入结论 → 03-判读库
- **新增判据**:`403 反枚举默认分支`——资源不存在→无差别 403(非 404);用"合法资源+字符变体全同"判别,不是注入信号 → 03-判读库状态码矩阵后
- **修正**:错误消息参数名可能只是文案(实例:file_diff 消息 fromFileVersionId 真名 from_file_version_id)→ 01 游标节"坑"
- **本轮实战补记**:org_id(500/368ms 快崩)、user_ids(200/354ms 截断)时间维终审;file_followers/comments/versions 的 403 反枚举实测;versions 四层防护收口;file_diff 参数链破译(真名 from_file_version_id + migration_version + nodes_to_diff)

---

## 2026-09-10 · Figma 新面三(r196ai-an:admin_requests 全格收口,16 参数)

- **新增构造**:`V2 数字正则族`(数字位分野:类型解析器 vs `^\d+$` 严格正则——前导零/溢出宽容,+1/1.0/空白/全角全拒) → 01-SQL入参矩阵 V2
- **新增构造**:`V6 全量校验+元素级精确匹配`(任一元素非法整单拒;不归一化 trim/NUL) → 01-SQL入参矩阵 V6
- **新增送达**:`A5 框架序列化名转换`——camelCase 逻辑名经序列化函数转 snake_case;静默 200 第一嫌疑=参数名未被识别(非参数化安全) → 06-送达与过障 A 族
- **新增送达**:`D3 opaque token 结构化探针`(解码形态四发/深度边界/结构快筛/耗时头佐证浅层崩) → 06-送达与过障 D 族
- **新增判据**:`框架强参数层`错误指纹(Rails `Invalid query parameters: expected Array (got String)`;`got Rack::QueryParser::Params`=下标形态) → 03-判读库指纹表
- **新增判据**:`第二判据·耗时头同量级`(server-timing 与同会话 200/400 同量级=浅层崩、未到 DB) → 03-判读库五·补
- **新增判据**:`六·补 精确匹配(不归一化)`行(同名家族行为不可跨端点外推) → 03-判读库
- **修正**:`200 状态码`行补"参数名未被识别"第一嫌疑 + 双发排除法 → 03-判读库
- **新增案例**:admin_requests(opaque cursor + 双解析路径 + 精确白名单) → 05-实战案例 案例6
- **本轮实战补记**:cursor=opaque(base64+JSON/54+ 结构探针零差分/80↔120 层定案 max_nesting=100);结论文档 `figma_report/Figma-H1-admin_requests-r196-结论-2026-09-10.md`

---

## 2026-09-10 · Figma 收尾批(related_links/recent_activity/export + 全量整理)

- **新增判据**:`403 双模板细分`——no_permission(功能/计划门) vs no_permission_contact_org_admin(组织管理员/审计门);i18n id 字段最可靠;模板差异=门类型差异 → 03-判读库 一/二节
- **新增判据**:`校验顺序指纹`(参数值校验先于权限 vs 权限先于参数解析;取无权资源带非法参数/空 body 两发定序) → 03-判读库 二·补(新节)
- **新增判据**:`RST 瞬态`行(会话首 1-5 发 ConnectionResetError 10054=网络瞬态;重打即通;持续才升级) → 03-判读库 六 噪声清单
- **新增构造**:`批量端点静默宽容`(集合接口无差别 200 空零时延;判别面未激活不得写"参数化安全") → 01-SQL入参矩阵 V6
- **新增案例**:related_links/recent_activity/export 三面(计划门+静默宽容+校验顺序) → 05-实战案例 案例7
- **本轮实战补记**:related_links 批量 21 发全静默(含 pg_sleep 零延迟);单文件四端点自有文件也 403=Dev Mode 计划门;recent_activity 7 枚举真值+接受子集+参数先行;export org admin 门全封(空 body 也 403);整理三产物 `figma_report/Figma-SQL注入总账-2026-09-10.md` / `Figma-SQL请求解析源码汇总-2026-09-10.md` / `Figma-今日总结-2026-09-10.md`

---

## 2026-09-11 · Figma r197(值→标识符升格族三面收口 + 校准针 + 死参数判定)

- **新增方法**:`值→标识符升格判别(val-up 三态法)`——基线/自引用恒真/残缺语法崩;前置校准针(格式合法不存在值)先定 403 判别力 → 02-方法论 七(新节)
- **新增构造**:`路径位算术/残缺四连发`(`id-0`/`id+1`/`id.`/`id'`;to_i 截断 vs SQL 表达式判别矩阵) → 01-SQL入参矩阵 V2 路径位
- **新增判据**:`整数反序列化类型墙`指纹(字符串进整数位→400 `Non-number characters in quoted integer`) → 03-判读库 指纹表
- **新增判据**:`403 反枚举校准针`纪律(判别力=0 时只写"无信号+机制归属") → 03-判读库 反枚举段
- **新增判据**:`死参数(同名参数未被消费)`判法(多值差分全同+瞬态字段剥离) → 03-判读库 200 行
- **新增判据**:动态字段 `realtime_token`(每请求 ts+HMAC:`/file-<key>:<ts>:0:<hmac>`、`/folder-<id>:<ts>:0:<hmac>`) → 02 快照动态字段表
- **新增案例**:r197 升格族三面(versions/folders/folder_search) → 05-实战案例 案例8
- **本轮实战补记**:V 面自引用/残缺/尾引号/随机 key 全 403 同构(升格排除);folders path to_i 截断(`+1` 200 同基线);query folderId 死参数(7 变体全同);结论文档 `figma_report/Figma-SQL注入-r197-val-up-结论-2026-09-11.md`

---

## 2026-09-11 · 补记 Figma r159-r165(跨字段缝合否决 · 当时漏登记,从会话存档回流)

- **新增构造**:`跨字段缝合构造`(多参拼一段文本:参数1=`/*`、参数2=`*/`;引号对;头/尾符号矩阵)——实测全崩,各参独立过解析门、无共享 SQL 文本 → 01-SQL入参矩阵 附节
- **新增判据**:`缝合否决判据`(跨参组合行为=各槽单发之和=参数化铁证) + `头/尾净化不对称`(头剥 `/` `'`、尾崩) + `重复键三态`(before/ps 取尾;sb 重复=支路丢弃;`x[]=` 数组语法另属类型崩) → 03-判读库
- **本轮实战补记**:server 游标 prev/next URL 原样回打逐字同构(默认 ps=30、51→钳50);FK2 游标×IHt8 路径→无跨文件泄漏;验证矩阵 15 格全绿(400/403 均业务校验,无 500)

---

## 2026-09-11 · Figma file_diff/checkpoint_diff 收口(r198-r208:稳定 500 端点 + 形态维归因)

- **新增送达**:`D4 请求形态复刻排除法`——从 JS 挖客户端头构造点→全量补齐重打 + 逐头减法;全补齐不变即排除"请求不像官方客户端"假设;版本头真值=页面内联 release_manifest_git_commit,计划头值=cookie 解码(uhp) → 06-送达与过障 D 族
- **新增判据**:`CDN 边缘透传标记`行(X-Cache: Error from cloudfront + Via *.cloudfront.net + 应用层 JSON 错误体 = 源站 5xx 透传、归因源站) → 03-判读库指纹表
- **新增判据**:`500 归因第三维·形态维`(请求头复刻排除;时间维/耗时头维之外) → 03-判读库 五·补
- **新增判据**:`数字位前缀解析(to_i 语义)`行(id 位尾接语法全同响应+布尔对零分叉;0 OR 1=1→404=关联门证据) → 03-判读库 六·补
- **新增判据**:`稳定 500 端点(无 200 基线)可收口`(全合法组合恒 500 + 全注入形态零差分 + 三维归因 = 业务层故障、注入路径不可达) → 03-判读库 500 行
- **新增构造**:`V2 关联 id 型数字位`(外键/关联 id:引号/注释/布尔/时延四族全负画像) → 01-SQL入参矩阵 V2
- **新增案例**:file_diff/checkpoint_diff(校验链五层:路由 405 / fk 网关 403 / dv zod 400 / 关联 404 / 业务 500;全客户端头复刻;全格全负) → 05-实战案例 案例9
- **本轮实战补记**:checkpoint_diff 全 500 归因(形态维/数据维/时间维三重排除);客户端头四件套实证(X-Csrf-Bypass:"yes"/tsid/Client-Version 真值/Plan-Max:starter);manifest 真值提取法;admin 面板路径 404 屏蔽;结论文档 `figma_report/Figma-file_diff-checkpoint_diff-审计结论-2026-09-11.md`

---

## 2026-09-11 · Figma r209(canvas_mentions / code_connect in_context / voting_sessions 三新面收口)

- **新增方法**:`写面门污染与净化纪律`——写面资源状态门被前序未清理样本占用 → 后续全假 400 泛化文案;纪律=逐发 create→测试→清理、门先验两发、净化重测、阴性对照 → 03-判读库 六·补二(新节)
- **新增判据**:`业务门 400(写面泛化)`行(400 通用文案无参数名=业务状态门,非参数错) → 03-判读库 指纹表
- **新增判据**:`第四判据·类型空转反证`(同语义合法类型值也崩+零时延+截断旁证=崩在方法链非 SQL;`"2"` 走 SQL 必成功) → 03-判读库 五·补
- **新增判据**:`假 400(写面门污染)`噪声行(整片泛化 400+耗时升=先疑门后疑 payload) → 03-判读库 六
- **新增判据**:`写面 id 位(to_i 写面版)`行(尾缀吸收+动作真生效副作用验收+阴性对照) → 03-判读库 六·补
- **新增构造**:`V1 写面回显法`(写面 payload 存储态直读=参数化铁证,少一层读面歧义) → 01-SQL入参矩阵 V1
- **新增构造**:`V2 写面路径位/关联位 to_i 版`(`/idabc` `/id'` 尾缀+副作用验收;`id_to_end` 三态全等) → 01-SQL入参矩阵 V2
- **新增构造**:`V4 类型空转`(字符串进标量数字位→500 零时延;"同语义合法类型值也崩"排除 SQL) → 01-SQL入参矩阵 V4
- **新增构造**:`V6 集合位元素注入静默`(asset_keys 元素全 200 空;判别面未激活纪律) → 01-SQL入参矩阵 V6
- **新增案例**:r209 三新面(canvas_mentions/in_context/voting_sessions) → 05-实战案例 案例10
- **本轮实战补记**:in_context 响应键 `"library_key,asset_key"` 复合结构(未来激活预埋);投票 body 全字段 chunk 2b17fec9;needs_invite 差分激活(同码异 reason);写面回显铁证;门机制四步验证+阴性对照;净残留 0;结论文档 `figma_report/Figma-SQL注入-r209-三新面-结论-2026-09-11.md`

---

## 2026-09-11 · Figma r210(mark_diagram_inserted / claim / apply_hub_file_collections 三遗留端点收口)

- **新增方法**:`路由与前置可达性判别`——404 三形态学(SPA 巨型 HTML 兜底=方法/路径未进 API;纯文本 `Not Found`=路由级;JSON `error.not_found`=业务级)+ 活性探针(OPTIONS/错误方法/坏 JSON 三发)+ **前置门判死法**(真值也拿不到非门响应⇒注入判别面不可建立) → 03-判读库 二·补二(新节)
- **新增判据**:`前端 SPA 兜底页`行(404+巨大 HTML+前端 bundle=路由未匹配被静态站点回退) → 03-判读库 指纹表
- **新增构造**:`数字位清洗型/拼接型仲裁`(真值+尾缀吞没/AND 布尔对零分叉/前缀法三针对;(200,200)=清洗判死、(404,200)=拼接升格;禁 OR 形态) → 03-判读库 六·补
- **新增构造**:`V1 路径 key 位镜像判据`(真 key+引号族全 403=精确匹配;若清洗吞回应得 200 基线;同请求两位解析器对照) → 01-SQL入参矩阵 V1
- **新增构造**:`V2 真值清洗型识别(parseInt 仲裁三针对)`(尾缀吞没/布尔对零分叉/`abc` 前缀败=parseInt 实锤;必须用真值取证) → 01-SQL入参矩阵 V2
- **新增案例**:r210 三遗留端点(活性探针首用+镜像判据+parseInt 仲裁) → 05-实战案例 案例11
- **本轮实战补记**:三面全负(前置门×2+parseInt 清洗×1);markInserted 真值也 404=AI job 前置门(路由活性已证);claim 统一 400 文案=workshop 门;apply_hub body `source_hub_file_id` 真值+`'`→200 但布尔对零分叉=清洗;FK2 6 次 200 后 WS 集合空(零痕迹,RuEdaoh 对照);404 三形态共存;r211 候选=hub_files 族(copy/prepare_insert/template_canvas/resource_uses)+templates 族+`files/{fk}/workshop`;结论文档 `figma_report/Figma-SQL注入-r210-三遗留端点-结论-2026-09-11.md`

---

## 模板(后续项目回流时复制)

```
## YYYY-MM-DD · 项目名
- 新增技术/构造:XX(打法/判据/坑)→ 01-入参矩阵(槽位 payload 库)/ 06-送达过障(通道族)追加编号
- 新增判据:XX → 03-判读库
- 新增案例:XX → 05-实战案例
- 修正/废弃:XX(原因)
```
