# 03 · 判读库(响应语义与防御层识别)

> 目的:把"响应"翻译成"防御架构信息"。判读统一走本表,避免过度解读或无谓重复。

---

## 一、错误消息指纹表(按拦截层归档)

| 层 | 消息格式特征 | 实例 | 含义 |
|---|---|---|---|
| 边界层(CDN/WAF) | **HTML** 错误页(非 JSON)| `<!DOCTYPE HTML ... <TITLE>ERROR` / 400 | 请求在到达应用前被拦(常为 null/畸形 URL) |
| 路由解析层 | `Error in route typechecking for /api/xxx: Error parsing JSON @L:C: Expected string`；整数位收到字符串→`Error parsing JSON @L:C: Non-number characters in quoted integer` | Figma `query[]=` / 非 string value；`org_id=org_id`(字符串进整数位) | **声明式参数校验**(JSON 解析级)先于业务——最强防御信号 |
| 框架强参数层 | `Invalid query parameters: expected Array (got String) for param 'x'`(
`got Rack::QueryParser::Params` = 下标形态) | Figma `request_types` 裸名混发 | Rails ActionController 强参数(expect/permit);**数组参数有两种解析路径**:裸名=JSON typecheck / `[]`名=数组收集 |
| schema 校验层 | `'x' must be a valid number, received type String` / `Required field 'x' is missing` | Figma org_id/前导零 | 类型/必填校验(比解析层晚,比业务早) |
| 参数映射层 | 类型消息暴露**内部名**(消息里的名字≠请求里的名字) | Figma `file_type[]=`→"editor_type must be a string, received Array" | 外部名→内部名映射存在;数组语法是逼出内部名的工具(C2) |
| 白名单校验层 | `Invalid <param> parameter` | Figma `Invalid sort_column parameter` | 值级白名单;枚举外全拒(与"静默回退"模式相对) |
| 游标校验层 | `Invalid column type in cursor object` / `Endpoint requires secondary_column cursor to be set` | Figma versions(column/secondary_column) | keyset 分页游标对象的列名/组合校验;SQL 前拦截(死位) |
| 应用业务层 | 标准 JSON:`{"error":true,"status":404,...}` / 403 "You don't seem to have permission" | 404/403 业务语义 | 已到业务代码,权限/资源查找语义 |
| 业务门(写面泛化 400) | 400 通用 i18n 文案(如 `Bad request.`),**不含参数名/字段名**——与解析层/schema 层"指名道姓"消息相对 | Figma voting_sessions 第二发 create(同文件已有活跃会话)→400 `Bad request.`,伴耗时升高(1.0-1.6s) | **写面资源状态门**:参数合法、被业务状态拦;写面矩阵遇泛化 400 先疑门后疑 payload(见 六·补二) |
| 权限门(双模板细分) | 403 消息 A:"You don't seem to have permission to do that."(`error.no_permission`)/ B:"...Contact your organization administrator."(`error.no_permission_contact_org_admin`) | Figma related_links 单文件(功能/计划门=A) vs activity_logs/export(组织管理员/审计门=B) | **两种门语义不同**;模板差异=门类型差异(i18n id 字段最可靠) |
| 数据库层 | `SQLSTATE 22021` / `CharacterNotInRepertoire` / `invalid byte sequence` / `Integer overflow` | PG 错误回显 | **值到达数据库的物理证据** |
| 语言运行时 | `NoMethodError: undefined method 'x'` / `can't quote Array` / `TypeError` | Ruby 翻译器崩溃 | 对象方法链/quote 环节的物理证据 |
| CDN 边缘透传标记 | 响应头含 `X-Cache: Error from cloudfront` + `Via: *.cloudfront.net`,但响应体是应用层 JSON 错误 | Figma checkpoint_diff 5xx | **源站 5xx 经 CDN 透传**(CDN 自身错误应是无应用 JSON 的 HTML/纯 5xx);应用 JSON+此头=后端业务错误,归因源站 |
| 前端 SPA 兜底页 | 404 但返回**全站 HTML**(体积巨大,K 级~百 K 级;含前端 bundle 引用) | Figma r210:`/api/file/mark_diagram_inserted/{k}` 的 OPTIONS/POST→404 · 826KB HTML | **方法/路径未进 API 层**(API 路由不匹配→静态站点回退兜底);判读:HTML 兜底=该组合无 API 路由;纯文本 `Not Found`(9B)=API 路由级不存在;JSON `error.not_found`=已到业务层 |

**用法**:测试中每个新错误消息 → 当场归层 → 更新指纹表。层定位决定"这个信号值不值得追"。

---

## 二、状态码 × 场景语义矩阵

| 码 | 可能语义 | 判别方法 |
|---|---|---|
| 200 | 正常 / 静默忽略(写盲接口)/ "存活命中" / **参数名未被识别** / **同名参数未被消费(死参数)** | 对比 base len/内容;写接口看读回;**先排除 A5(序列化名/形态未识别)**:同语义恶意值双发(camel↔snake、`[]`↔裸名),静默 200 过半数是打在了未被读取的名字上;死参数判法=多值差分(自引用/算术/残缺/不存在/极小)全同结果 + 瞬态字段剥离后逐字节一致(实证:folders paginated_files query folderId) |
| 400 | 解析层拒 / schema 拒 / 业务参数拒 | **看消息文本**(@L:C 解析格式 vs 类型消息 vs 业务消息) |
| 403 | 存在但无权 / 防枚举统一应答 | 与 404 对照:同位置"存在+无权"vs"不存在"是否可区分 |
| 404 | 资源不存在 / 端点不存在 / 路由不匹配 | 用已知合法资源打对照;消息格式(JSON vs HTML) |
| 500 | 未捕获异常:null/类型/权限/重复键 | 隔离变量(每次只改一个字段)找根因;**并做时间维+形态维反证(见五·补)**;**全合法组合恒定 500 的端点(无 200 基线)可收口**:全注入形态零差分即判“业务层故障,注入路径不可达”(实证 checkpoint_diff) |
| 200 空集 | 参数被清洗后无匹配 / 参数被忽略 | 与"命中"值对照;变换参数是否影响结果 |

**403 反枚举默认分支(重要)**:资源不存在时可能**不走 404 而走 403**(防枚举设计)——实例:Figma file_followers(未知 key→403 `You don't have permission to view this prototype`)、comments(→403 `comments feature`)、versions(未知 fileKey→403 `permission to view this file`)。**判别**:用"已证合法资源"与其字符变体(引号/大小写/裁剪/加尾字符)各打一发——**行为完全一致→反枚举默认分支(不是注入信号)**;行为分叉→真权限边界。**校准针(2026-09-11 增,前置必打)**:先用"格式合法但不存在值"(随机同构 key / 相邻 id)打一发——与既有 403 全同构=默认分支,该位**判别力=0**;此时结论只能写"无信号+机制归属",不得写"参数化安全"(见七)。

**403 双模板细分(重要)**:同一项目可并存两种 403 消息模板——A `You don't seem to have permission to do that.`(i18n `error.no_permission`;功能/计划/资源门)vs B `You don't have permission to do that. Contact your organization administrator.`(i18n `error.no_permission_contact_org_admin`;组织管理员/审计门)。**模板差异=门类型差异**,i18n id 字段最可靠。用途:①快速定性"这面被什么门挡"(功能计划门 vs 组织管理员门);②计划门可换账号解锁,管理员门记录即可。实例:Figma related_links 单文件(自有文件也 A=计划门);activity_logs/export(空 body 也 B=**权限先于参数解析**)。

---

## 二·补 校验顺序指纹(参数校验 vs 权限校验,逐端点实测)

同一项目内不同端点的校验顺序不同,决定"在无权资源上能否做参数层探测":

| 顺序 | 表现 | 实例 |
|---|---|---|
| 参数值校验先于权限 | 无权资源+非法参数→400(参数消息);无权资源+合法参数→200/进权限 | Figma recent_activity(foreign+非法枚举=400 `Unsupported activity_type`;foreign+合法枚举=200) |
| 权限先于参数解析 | 空 body/任意参数→403(权限消息),参数层完全不可达 | Figma activity_logs/export(空 body 也 403 组织管理员门) |

**判法**:取"无权资源"各发两发——①带非法参数值 ②空 body——比消息类型即可定序(参数消息先现=参数先行;权限消息先现=权限先行)。
**用法**:①参数校验先行→可在无权资源上无害探枚举/参数行为(信息量极低但可写画像);②权限先行→参数层完全不可达,换账号前别投入。

---

## 二·补二 路由与前置可达性判别(2026-09-11 增,新端点首探必做)★

新面首探先回答"这面是否可达",再谈注入:**404 三形态学**(方法/路径是否进 API 层)+ **活性探针** + **前置门判死法**。

**404 三形态(按体积与内容定性)**:

| 形态 | 特征 | 含义 |
|---|---|---|
| 前端 SPA 兜底页 | 404 + 巨大 HTML(含前端 bundle);OPTIONS/POST 打到"疑似路径"同响应 | 该**方法/路径组合未进 API 路由**,被静态站点回退兜底 |
| 纯文本 `Not Found` | 404 极小体(<50B 纯文本) | **API 路由级不存在**(无此路径) |
| JSON `error.not_found` | 404 JSON 结构 | 已到**业务层**,资源/业务查找失败 |

**活性探针(判断目标方法路由是否已接)**:同一路径三发——①OPTIONS ②错误方法(打 PUT 面发 POST) ③目标方法+**坏 JSON**(`{`)——第 ③ 发若得 400 `Failed to parse body as JSON`,则 **body 解析中间件已挂 + 路由存在**(即使业务全 404)。

**前置门判死法**:**真值(真实存在资源)也拿不到非门响应** ⇒ 被**前置条件门**挡(资源状态/在途 job/模式门)——非路由问题、非判别面问题;此状态下注入族全 404/400 同构**不构成"无注入"证据**,收口只能写"**注入判别面不可建立(前置门)**"并判死。实证:Figma r210——mark_diagram_inserted 真值 FK2/k7tno 全 404(前置=AI diagram job 在途;活性探针已证路由存在);claim 全 400 统一文案(前置=workshop/try 文件模式)。**判死前必先区分**"路由不存在(SPA 兜底)"与"路由存在但前置未满足(JSON 404/业务 400)"——两者收口措辞不同。

---

## 三、结果三分法(每个 payload 必须归类)

```
① 崩(5xx / SQL 错误 / 运行时异常)
   → 进入本节指纹表归层;找根因(哪个字符/字段引发的)
② 空集(与空集基准一致)
   → payload 语义 = "一个找不到的词";对判别面贡献为零
③ 存活命中(与 base 相同,即"清洗后=原值语义")
   → 清洗层存在(剥掉了 payload 的语法部分);记录清洗范围:
     例:"引号/注释/分号/括号全剥,字母数字保留" → 说明白名单式清洗
```

**关键认知**:"存活命中"不是"注入失败",是**清洗层行为画像**。多个 payload 的画像拼起来 = 清洗规则反推。

---

## 三·补 防御处置四模式(同一层的不同取舍)

| 模式 | 表现 | 实例 | 含义 |
|---|---|---|---|
| 白名单拒绝 | 枚举外值→400 明确消息 | `sort_column=junk`→`Invalid sort_column parameter` | 校验前置、零宽容;白名单集合可用二分枚举 |
| 静默回退 | 枚举外值→200 走默认 | `file_type=junk`→200 全量 | 宽容取舍;**值在识别失败即弃,未进查询** |
| 清洗存活 | payload→200 与 base 相同 | folder_search 引号族 | 剥离式清洗;需跟踪存活字符集 |
| 类型拒绝 | 类型/语法错→400 类型消息 | `page_size=abc`→`must be a valid number`;`file_type[]=`→`received Array` | 解析层类型系统;数组语法可逼出**内部字段名** |

**判读原则**:同一参数上先定模式,再选打法——白名单模式做二分枚举(确定集合);静默回退模式试"能否让识别失败变成识别成功"(编码/剥离/截断变体);清洗模式做存活字符集测绘。

---

## 四、读回验证语义

| 存储形态 | 判定 | 安全性 |
|---|---|---|
| 输入原样(`x' OR '1'='1--` 原样) | bind 或完全等价转义 | ✅ 安全(两者等价,不必强分) |
| `''` 双写(`x'' OR ''1''...`) | 应用层做了 SQL 转义后拼串 | ✅ 安全(转义正确);记录"存在转义层" |
| 被剥离(`x OR 11`) | 清洗层拼接前剥离 | ⚠️ 危险候选:若剥离+拼串组合错误可注入;继续布尔对测试 |
| 被截断 | 长度限制 | 记录;结合溢出测试 |

**原则**:读回只能证明"存储态";注入可能性的最终裁决永远是**行为测试**(布尔对/时间)与**物理证据**(quote 错误)。

---

## 五、物理证据清单(值得主动追求的)

| 证据 | 制造方法 | 证明了什么 |
|---|---|---|
| `can't quote Array` 类错误 | A1 数组语法喂进值位 | 该值经过 quote 环节 |
| `SQLSTATE 22021`(null 字节) | B7 的 `\u0000` 到达 PG | 值**到达数据库**(参数或文本,不区分) |
| `table 'x' not defined in schema` | I1/I2 标识符位喂畸形 | 标识符进了**字典查询**(拼接嫌疑层) |
| 白名单回显 `Invalid value for X: yyy` | I3 枚举位 | 合法值列表泄露 + 校验存在 |
| 正则消息 `alias should match [A-Za-z]...` | I2 别名位 | 白名单正则防御存在 |
| `Integer overflow` | V2 数字边界 | 类型/范围校验层存在 |
| 跨参组合行为=各槽单发之和 | 跨参注释对/引号对(参数A=`/*`、参数B=`*/`) | **无共享 SQL 文本**⇒全参数化铁证;缝合轴关闭(Figma versions r161,X1-X3 全 500) |

---

## 五·补 500 归因:时间维判别(必做步骤)★

**规则**:任何"500/SQL 疑似"必须在时间维度反证——把 `pg_sleep(3)` 嵌进**语法完全合法的表达式**再送(如 `1 AND 1=(SELECT 1 FROM pg_sleep(3))`,尾接 `--` 确保拼接假设下语法闭合):
- 慢(≥3s)= SQL 真的执行 → 注入实锤;
- 快(<1s)= 崩在解析/校验层(整数溢出、日期解析、null 字节、类型断言),**不是 SQL**。

**原因**:500 有太多平凡来源;只有时间侧信道能区分"解析崩"与"SQL 崩"。**没有时间维证据的 500 不得写入注入结论**。
**实证**:Figma org_id `1 AND 1=(SELECT 1 FROM pg_sleep(3))`→500/368ms(parse 层崩);user_ids 同 payload→200/354ms(parseInt 截断,无执行);versions before→500/412ms(游标解析崩)。三处全部以"快"否决拼接。
**纪律**:每个位 2-3 发;基线对照取同会话近邻请求;>2500ms 记慢。

**第二判据·耗时头同量级(浅层崩佐证)**:服务端返回 timing 头(如 `server-timing: app;dur=`、`X-Runtime`)时,对照"通用 500"与同会话 200/400 的耗时——**同量级(<400ms 级)= 崩点在早检查/解析层,未到 DB 查询**;若 500 显著慢于基线 = 崩点更深,值得继续追。实证:Figma admin_requests cursor 全部 500 的 app;dur(124-385ms)与 200/400 应答同量级,佐证浅层崩(早检查拒绝)。

**第三维·形态维(请求头复刻排除,2026-09-11 增)**:当 500 疑似"服务端按请求形态分流(缺客户端头/版本头/CSRF 头)"时——从前端 JS bundle 挖出目标 API 客户端的头构造(搜 `extraDefaultHeaders`/`X-Csrf`/axios 实例工厂),全量补齐重打 + **逐头减法对照**:①客户端版本头真值=页面内联 `release_manifest_git_commit`(GET 文件页 HTML 正则提取);②计划头值=cookie 解码(如 `figma.reporting`→`uhp":"starter"`)。判据:全补齐后行为不变 ⇒ "请求不像官方客户端"假设排除,500 归属服务端数据/业务侧;逐头减法无差异=各头均非判别因素。实证:Figma checkpoint_diff——X-Csrf-Bypass:yes/tsid/X-Figma-Client-Version(真值)/X-Figma-User-Plan-Max 全补齐 500 不变,减法零差异。

**第四判据·类型空转反证(2026-09-11 增)**:500 疑似"值以字符串进 SQL 插值"时——**把同语义合法类型值再送一遍**:数字位收字符串 `"2"` 也 500 ⇒ 若为 SQL 插值,`"2"` 应成功入库(数字字面量合法),**排除 SQL 执行**;崩点在应用层方法链(如 Ruby `String#floor` NoMethodError=类型未进断言、在后续方法调用上崩)。旁证:数字位对浮点做截断(1.5→1=`.floor`/`.to_i` 型强转)。判据组合:①同语义合法类型值也崩(排除"内容触发")②零时延(排除 DB 往返)③截断旁证。实证:Figma voting `user_vote_limit`——1→200、1.5→截断 1、`"2"`/`"1+1"`/`"1'"`→500 零时延;定性=类型空转,作为"500≠注入"标准反例。

---

## 六、噪声清单(别当信号的东西)

| 噪声 | 表现 | 处理 |
|---|---|---|
| score/相关性浮点 | 同 payload 两次 len±1~3 | 归一化后再 diff |
| 时间戳字段 | updated_at 变化 | 固定快照比较 |
| 列表序漂移 | 数据变动时整个 body 变 | 同窗口内比;逐字节定位首差异 |
| 网络抖动 | 计时 ±300ms | 时间盲注用 3+3 组统计 |
| **连接重置(RST)** | `ConnectionResetError(10054)`——**会话首 1-5 发**偶发,后续全正常 | 网络层瞬态,非内容/权限触发;重打确认——重打即通=瞬态,持续 RST 才升级排查 |
| 编码显示差 | `\` 与 `"` 在 grep/正则输出中的转义显示 | 落盘后按原始字节核对 |
| **假 400(写面门污染)** | 写面 create 类:前序样本未清理致资源"已存在",后续发全被业务门挡 400 **泛化文案**(如 `Bad request.`)——矩阵整片污染 | **逐发 create→测试→清理**(不攒脚本末尾);开测前门探针(最小合法双发);"某字符族集体泛化 400"先疑门后疑 payload;见 六·补二 |

---

## 六·补 归一化行为(预处理证据)

| 行为 | 探法 | 实证 | 含义 |
|---|---|---|---|
| trim | `' value'` / `'value '` | `' touched_at'` 过白名单 | 匹配前 strip(预处理) |
| **精确匹配(不归一化)** | 尾空格/NUL/大小写变体 | `account_type_request ` 拒(对比 `credits_used ` 放行) | **同名家族行为不可跨端点外推,逐端点独立确认** |
| \x00 完全剥离 | **中间**插入 `des\x00ign` | →design 命中;`name\x00` 过白名单 | 剥离(非截断);值被重构后再匹配 |
| 大小写严格 | `DESIGN` / `Design` | 拒绝/回退(不识别) | 大小写敏感白名单 |
| 严格单层解码 | `%2527` 类 | 单层解码后不再解 | 防双重编码链 |
| **头/尾净化不对称** | 符号置头 vs 置尾 | before:`/` `'` 头剥(200/n=0)、尾崩;`+ - . --` 头尾皆崩 | 净化=解析器自带剥除器(仅头尾特定符号集);槽宽容度不可外推 |
| **同键重复取尾(last-wins)** | 同键两值 `before=[A,B]` | `[1970,2030]`→n=0、`[2030,1970]`→n=1;**但重复 sb=支路整体丢弃** | 取尾是解析层行为,**逐参数独立确认**;`x[]=v` 数组语法另属类型崩 |
| **数字位前缀解析(to_i 语义)** | 关联 id 位尾接语法:`<id>'` / `<id>' AND '1'='1` / `<id>' AND '1'='2` | checkpoint_diff ffv:引号/布尔族全 500 同响应;`0 OR 1=1`→404(解析 0→查无) | 前缀数字提取、尾部语法被吸收;**布尔对零分叉=排除拼接**;解析值参与关联查询(0→查无=关联门证据) |
| **写面 id 位(to_i 写面版)** | 写面路径/关联位尾接语法:`/idabc` / `/id'` / `/id'--`;**验证"动作真生效"**(读面看响应,写面看副作用) | r209 voting:PUT/DELETE 路径尾缀三态全 200 且会话真被结束/删除(plain 复删 404);`id_to_end` plain/`'`/`'--` 三态全等(B 建成功=A 被结束)+不存在 id 阴性对照(门 400) | 写面同族:前缀解析+尾部吸收,解析值**真参与写操作**;写面验证法=副作用验收(结束/删除/计数变化),非响应文案 |
| **数字位清洗型/拼接型仲裁(2026-09-11 增)** | 数字语义位(数字字符串 schema):**真值**+语法尾缀族(`'`/`''`/`abc`/布尔对) | r210 apply_hub `source_hub_file_id`:真值+`'`/`''`→200(尾缀被吞);真值+`' AND '1'='2` vs `' AND '1'='1`→200×2(布尔对零分叉);真值+`abc`→200 vs `abc`+真值→404=parseInt 前缀语义实锤 | **必须用真值取证**(不存在值上引号族全同构=无判别力);(200,200)=清洗型判死,(404,200)=拼接升格;**禁 OR 形态**(数字位拼接=全表写风险,只用 AND 布尔对) |

**用法**:这些行为都是"值被应用层处理过"的物理证据——与"原样拼接进 SQL"不相容;但注意**不能用它区分 bind 与清洗**(两者都先处理值)。

---

## 六·补二 写面门污染与净化纪律(2026-09-11 增,写面矩阵必读)★

写面端点(create/update)常有**资源状态门**(同资源只允许一个活跃对象/状态机限制),被门挡时返回**泛化 400 文案**(如 `Bad request.`),与 payload 内容无关。前序样本未清理 → 后续矩阵**整片假 400**,会误判"服务端拒绝特殊字符"。

**纪律**:
1. **逐发闭环**:每发 = create → 测试 → 清理(PUT 结束/DELETE),不留活跃样本跨发;禁止把清理攒到脚本末尾(中断=全矩阵污染)。
2. **门先验**:开打前"最小合法 create + 第二发 create"两发确认门行为(第二发泛化 400=门存在)。
3. **污染识别**:出现"某字符族集体泛化 400 + 耗时升高"→ 先疑门污染,后疑 payload;**净化重测**再定性。
4. **阴性对照必打**:门验证含"不存在的目标 id"发(如 `id_to_end=999999`)——B 被门拦(=A 未被结束)⇒ 证明解析值真参与关联操作。

**实证(r209 voting_sessions)**:首轮 7 发"title 拒绝引号/特殊字符"全部是门假象(create 后未即时清理);净化重测后同 payload 全部 200 原样入库。门四步:G1 create 200 → G2 第二发 create 400(1.6s) → G3 PUT 结束 200 → G4 create 200。阴性对照:E4 不存在 id_to_end → B 被门拦(证明 A 未被结束)。

---

## 七、"未发现注入"的表述标准

允许说:
- "本位置按入参矩阵(槽位 × 通道)逐条打完,无注入行为信号"(附对账表)
- "防御形态识别为:X 层校验 + bind/等价转义"

禁止说:
- "绝对没有 SQL 注入"(黑盒无法证明绝对)
- "打满了"(除非对账表全条覆盖)

**收口措辞模板**:
> 位置 X 参数 Y:矩阵(槽位 V1-V6/I1-I4/F1/M1/X1 + 通道 A/B/C)全条已打(不适用项已注明);判别面激活(三态可辨);全部 payload 归入"存活命中/空集/解析层拒",无崩无差分无物理证据泄露;判定:无公开可利用的 SQL 注入行为,防御=N 层(具体机制)。
