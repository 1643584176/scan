# 全局经验使用说明

把你认为重要、适用于所有项目的 Markdown 文件直接放进这个目录或其子目录。

- 文件名和正文格式不限，不需要 Front Matter。
- 所有 Markdown 默认都是全局、必学、用户维护的经验。
- 程序只读取原文，不会自动修改、重排或删除这些文件。
- 内容哈希、加载凭证和 AI 派生草稿只保存在本地 SQLite，不提交 Git。
- `说明.md` 只用于说明，不会被当作经验正文加载。

原有 `经验学习1–7.md` 和索引已经迁移到这里；以后新增内容也统一放在这里。
